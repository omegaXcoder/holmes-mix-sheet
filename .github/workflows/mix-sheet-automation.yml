name: Mix Sheet Automation

on:
  schedule:
    # 8pm Mountain Time, same day, every day except Sunday (nothing runs Sunday, so
    # there's nothing to record that evening).
    #
    # 02:00 UTC = 8pm MDT (Mountain Daylight Time, UTC-6). This is a fixed UTC time, not
    # DST-aware, so during Mountain Standard Time (winter, UTC-7) this actually runs at
    # 7pm local - accepted as fine, same tradeoff as before.
    #
    # IMPORTANT: 8pm local crosses midnight UTC, landing on the NEXT UTC calendar day - so
    # the day-of-week to skip is NOT the UTC day matching "Sunday". Business Sunday 8pm
    # MDT = Monday 02:00 UTC, so the UTC day-of-week that must be skipped is Monday (1).
    # Day-of-week: 0=Sun,1=Mon,2=Tue,3=Wed,4=Thu,5=Fri,6=Sat - this list (0,2,3,4,5,6) is
    # every day EXCEPT Monday in UTC terms, which is every business day EXCEPT Sunday.
    - cron: '0 2 * * 0,2,3,4,5,6'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Scrape and compute everything but skip writing to the sheet'
        type: boolean
        default: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - name: Write service account key
        run: echo '${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY_JSON }}' > service-account-key.json

      - name: Run automation
        env:
          SA_EMAIL: ${{ secrets.SA_EMAIL }}
          SA_PASSWORD: ${{ secrets.SA_PASSWORD }}
          GOOGLE_SERVICE_ACCOUNT_KEY_PATH: ./service-account-key.json
          MIX_SHEETS_FOLDER_ID: 1KsCqKKnZOTAJbkXRf80ZT1JLF3ul-pzN
          MIX_SHEET_TEMPLATE_ID: 1g8E39SjoRwQUH-mmUUf0F7TMLUX7KC8cOVyxwKyX3bE
          BUSINESS_TIMEZONE: America/Denver
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
          HEADLESS: 'true'
          SMTP_HOST: smtp.gmail.com
          SMTP_PORT: '587'
          SMTP_FROM_EMAIL: ${{ secrets.SMTP_FROM_EMAIL }}
          SMTP_APP_PASSWORD: ${{ secrets.SMTP_APP_PASSWORD }}
          NOTIFY_EMAIL_TO: omega@kudos.marketing
        run: npm start
